package enums;

/**
 * Teacher academic positions.
 * Requirement: "use enumerations to represent teachers' position —
 * tutor, lector, senior lector, professor, etc."
 */
public enum TeacherPosition {
    TUTOR, LECTOR, SENIOR_LECTOR, PROFESSOR
}
